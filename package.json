{
  "name": "astorga-propuesta-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Deck de propuesta comercial — Landing Page para Astorga Building Company",
  "scripts": {
    "build:css": "tailwindcss -i theme.css -o tailwind.css --minify",
    "watch:css": "tailwindcss -i theme.css -o tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
